# Backend API URL — set this when deploying the frontend independently
# For local dev, the default (http://localhost:3000/api) is used if this is unset
VITE_API_URL=https://your-backend.onrender.com/api
